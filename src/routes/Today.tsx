import { PagePlaceholder } from '@/components/PagePlaceholder'

export function Today() {
  return (
    <PagePlaceholder
      title="Today"
      blurb="What's overdue, what's due, and who we're waiting on. The home screen and the reason the tool exists."
    />
  )
}
