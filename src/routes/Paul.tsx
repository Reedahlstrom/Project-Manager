import { PagePlaceholder } from '@/components/PagePlaceholder'

export function Paul() {
  return (
    <PagePlaceholder
      title="Paul"
      blurb="The three things only Paul can do this week, decisions waiting on him, and one line of health per project. Readable in ninety seconds on a phone."
    />
  )
}
