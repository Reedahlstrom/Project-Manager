import { PagePlaceholder } from '@/components/PagePlaceholder'

export function Scorecard() {
  return (
    <PagePlaceholder
      title="Scorecard"
      blurb="Computed, never self-reported. On-time rate, Paul's overdue count, how fast notes get captured, and what's been left waiting."
    />
  )
}
